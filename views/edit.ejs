<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>프로젝트 수정</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="card shadow p-4 mx-auto" style="max-width: 600px;">
            <h2 class="mb-4 text-primary">프로젝트 수정하기</h2>
            <form action="/edit/<%= project.id %>" method="POST">
                <div class="mb-3">
                    <label class="form-label">프로젝트 제목</label>
                    <input type="text" name="title" class="form-control" value="<%= project.title %>" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">이미지 URL</label>
                    <input type="text" name="image_url" class="form-control" value="<%= project.image_url %>">
                </div>
                <div class="mb-3">
                    <label class="form-label">설명</label>
                    <textarea name="description" class="form-control" rows="5" required><%= project.description %></textarea>
                </div>
                <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                    <a href="/project/<%= project.id %>" class="btn btn-secondary me-md-2">취소</a>
                    <button type="submit" class="btn btn-warning text-white">수정 완료</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>