<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>내 포트폴리오</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container py-5">
        <header class="mb-5 d-flex justify-content-between align-items-center">
            <div>
                <h1 class="fw-bold">My Portfolio</h1>
                <p class="text-muted">학번: 202245098 | 이름: 최우진</p>
            </div>
            <a href="/write" class="btn btn-primary btn-lg">프로젝트 등록하기</a>
        </header>

        <div class="row row-cols-1 row-cols-md-3 g-4">
            <% projects.forEach(function(project) { %>
                <div class="col">
                    <div class="card h-100 shadow-sm">
                        <img src="<%= project.image_url %>" class="card-img-top" alt="img" style="height: 200px; object-fit: cover;">
                        <div class="card-body">
                            <h5 class="card-title"><%= project.title %></h5>
                            <p class="card-text text-truncate"><%= project.description %></p>
                            <a href="/project/<%= project.id %>" class="btn btn-outline-dark w-100 mt-2">자세히 보기</a>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>