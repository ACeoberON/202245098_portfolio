<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>개인 포트폴리오</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container py-5">
        <header class="mb-5 text-center">
            <h1 class="display-4 fw-bold">My Portfolio</h1>
            <p class="lead text-muted">Node.js와 MySQL로 구축한 개인 포트폴리오 페이지입니다.</p>
        </header>

        <div class="row row-cols-1 row-cols-md-3 g-4">
            <% projects.forEach(function(project) { %>
                <div class="col">
                    <div class="card h-100 shadow-sm">
                        <img src="<%= project.image_url %>" class="card-img-top" alt="project image">
                        <div class="card-body">
                            <h5 class="card-title"><%= project.title %></h5>
                            <p class="card-text"><%= project.description %></p>
                        </div>
                        <div class="card-footer text-muted">
                            <small>ID: <%= project.id %> | <%= project.created_at %></small>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>