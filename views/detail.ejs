<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>상세 보기</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="d-flex justify-content-between mb-3">
            <a href="/" class="btn btn-outline-secondary">← 목록으로</a>
            <a href="/edit/<%= project.id %>" class="btn btn-warning">수정하기 ✏️</a>
        </div>
        <div class="card shadow">
            <img src="<%= project.image_url %>" class="card-img-top" alt="img" style="max-height: 400px; object-fit: cover;">
            <div class="card-body p-5">
                <h1 class="display-5 fw-bold mb-3"><%= project.title %></h1>
                <p class="text-muted">등록일: <%= project.created_at %></p>
                <hr>
                <p class="lead" style="white-space: pre-line;"><%= project.description %></p>
            </div>
        </div>
    </div>
</body>
</html>